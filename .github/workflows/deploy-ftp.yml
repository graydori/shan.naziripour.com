name: Deploy to SFTP

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Debug host
        run: echo "HOST=${{ secrets.SFTP_SERVER }} USERNAME=${{ secrets.SFTP_USERNAME }} PASSWORD=${{ secrets.SFTP_PASSWORD }}"
      - name: Deploy to SFTP
        uses: AbleLincoln/push-to-sftp@v1.0
        with:
          host: ${{ secrets.SFTP_SERVER }}
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: 22
          sourceDir: ./
          targetDir: ./home/graydori/shan.naziripour.com/